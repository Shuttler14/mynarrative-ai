[project]
name = "api-secondary"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.82.0",
    "Pillow",
    "requests",
    "supabase",
]

[tool.vercel]
entrypoint = "handler:handler"
